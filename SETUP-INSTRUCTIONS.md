# 🚀 ІНСТРУКЦІЇ ДЛЯ ЗАПУСКУ ПРОЕКТУ

## ⚠️ Необхідні компоненти

### 1. Встановлення Node.js

**Варіант 1: Офіційний сайт**
1. Завантажте з https://nodejs.org/ (LTS версія)
2. Запустіть інсталятор
3. Перезапустіть PowerShell/VS Code

**Варіант 2: Через winget (Windows 11)**
```powershell
winget install OpenJS.NodeJS.LTS
```

**Варіант 3: Через Chocolatey**
```powershell
choco install nodejs-lts
```

### 2. Перевірка встановлення

```powershell
node --version    # Має показати v20.x.x або новіше
npm --version     # Має показати 10.x.x або новіше
```

---

## 🎯 ЗАПУСК ПРОЕКТУ

### Варіант A: З Backend (Full Stack)

#### Крок 1: Встановлення MongoDB

**Опція 1: MongoDB Locally**
```powershell
# Завантажте з https://www.mongodb.com/try/download/community
# Або через chocolatey:
choco install mongodb

# Запустіть MongoDB
mongod
```

**Опція 2: MongoDB Atlas (Рекомендовано для початку)**
1. Зареєструйтесь на https://www.mongodb.com/cloud/atlas
2. Створіть безкоштовний кластер (M0)
3. Отримайте connection string
4. Використайте його в `.env`

#### Крок 2: Backend Setup

```powershell
# Перейдіть в папку server
cd server

# Встановіть залежності
npm install

# Створіть .env файл з прикладу
copy .env.example .env

# Відредагуйте .env файл в VS Code:
# PORT=5000
# MONGODB_URI=mongodb://localhost:27017/quiz-app
# або ваш MongoDB Atlas connection string

# Заповніть БД тестовими даними (необов'язково)
npm run seed

# Запустіть backend сервер
npm run dev
```

✅ Backend запущений на `http://localhost:5000`

#### Крок 3: Frontend Setup

```powershell
# Поверніться в кореневу папку (відкрийте НОВИЙ термінал!)
cd ..

# Встановіть залежності frontend
npm install

# Створіть .env в корені проекту
echo VITE_API_URL=http://localhost:5000/api > .env

# Запустіть frontend
npm run dev
```

✅ Frontend запущений на `http://localhost:5173` або `http://localhost:3000`

---

### Варіант B: Без Backend (LocalStorage)

Якщо не хочете налаштовувати backend, проект може працювати тільки з LocalStorage:

```powershell
# В корені проекту
npm install
npm run dev
```

**Увага:** Потрібно буде повернути старі версії Redux слайсів, які працюють з LocalStorage.

---

## 🔍 ПЕРЕВІРКА ЧИ ВСЕ ПРАЦЮЄ

### Backend перевірка:
```powershell
# В браузері або curl
curl http://localhost:5000/api/health
```

Має повернути:
```json
{
  "status": "ok",
  "message": "Quiz API Server is running",
  "timestamp": "..."
}
```

### Frontend перевірка:
1. Відкрийте http://localhost:5173
2. Має відкритись головна сторінка
3. Перевірте console (F12) на помилки

---

## 🐛 ТИПОВІ ПРОБЛЕМИ

### 1. `npm: The term 'npm' is not recognized`
**Рішення:** Встановіть Node.js (див. вище)

### 2. `Cannot find module 'express'`
**Рішення:** 
```powershell
cd server
npm install
```

### 3. `EADDRINUSE: address already in use :::5000`
**Рішення:** Порт 5000 зайнятий. Змініть PORT в `.env`:
```
PORT=5001
```

### 4. `MongooseError: Connection refused`
**Рішення:** 
- Переконайтесь що MongoDB запущений (`mongod`)
- Або використайте MongoDB Atlas
- Перевірте MONGODB_URI в `.env`

### 5. CORS помилки
**Рішення:** Переконайтесь що:
- Backend запущений на localhost:5000
- Frontend в `.env` має `VITE_API_URL=http://localhost:5000/api`

---

## 📱 ШВИДКИЙ СТАРТ (без встановлення)

Якщо хочете просто побачити як працює UI без backend:

```powershell
# Тимчасово відкотіть зміни в Redux слайсах
# Або використайте готові demo дані в компонентах
npm run dev
```

---

## 🎓 ДЛЯ ЗАХИСТУ

Якщо у вас немає часу налаштовувати MongoDB:

1. **Демонструйте код** - покажіть структуру, API endpoints, моделі
2. **Використайте скріншоти** - можна показати як має працювати
3. **Поясніть архітектуру** - важливіше розуміння, ніж запущений проект
4. **Альтернатива:** Використайте версію з LocalStorage

---

## 📞 ПОТРІБНА ДОПОМОГА?

1. Перевірте чи встановлений Node.js: `node --version`
2. Перевірте чи встановлений npm: `npm --version`
3. Перевірте логи в терміналі на помилки
4. Відкрийте Browser Console (F12) для frontend помилок
5. Перевірте чи запущений MongoDB (якщо використовуєте локально)

---

## ✅ CHECKLIST ПЕРЕД ЗАПУСКОМ

- [ ] Node.js встановлений (v18+)
- [ ] npm працює
- [ ] MongoDB запущений АБО є MongoDB Atlas connection string
- [ ] Backend залежності встановлені (`cd server && npm install`)
- [ ] Frontend залежності встановлені (`npm install`)
- [ ] .env файли створені (в server/ та в корені)
- [ ] Backend запущений (окремий термінал)
- [ ] Frontend запущений (окремий термінал)

---

**Готово! Проект має запуститись! 🚀**
