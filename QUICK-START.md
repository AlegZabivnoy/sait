# 🚀 Швидкий запуск після встановлення Node.js

## Кроки після перезапуску VS Code:

### 1️⃣ Перевірка встановлення
```powershell
node --version
npm --version
```

### 2️⃣ Встановлення залежностей Backend
```powershell
cd server
npm install
```

### 3️⃣ Налаштування Backend
```powershell
# Створіть .env файл
copy .env.example .env

# Відредагуйте .env (або залиште як є для локального MongoDB)
```

### 4️⃣ Встановлення MongoDB (опціонально)

**Швидкий варіант - без MongoDB:**
Можна запустити проект БЕЗ backend, використовуючи LocalStorage.

**З MongoDB:**
- Локально: https://www.mongodb.com/try/download/community
- Або MongoDB Atlas (хмара): https://www.mongodb.com/cloud/atlas

### 5️⃣ Запуск Backend (якщо є MongoDB)
```powershell
# В папці server
npm run seed    # Заповнити тестовими даними
npm run dev     # Запустити сервер
```

### 6️⃣ Запуск Frontend
```powershell
# В НОВОМУ терміналі, в корені проекту
npm install
npm run dev
```

### 7️⃣ Відкрийте браузер
- Frontend: http://localhost:5173 або http://localhost:3000
- Backend API: http://localhost:5000/api/health

---

## ⚡ Швидкий варіант БЕЗ Backend

Якщо не хочете налаштовувати MongoDB зараз:

```powershell
# Просто запустіть frontend
npm install
npm run dev
```

Проект працюватиме з LocalStorage (дані зберігаються локально в браузері).

---

## 🎯 Готово!

Після перезапуску VS Code, відкрийте термінал і почніть з перевірки:
```powershell
node --version
```

Якщо побачите версію Node.js - можна запускати! 🚀
