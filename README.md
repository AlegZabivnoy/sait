# Quiz Application - Full Stack React + Node.js

Повноцінний Full Stack додаток для створення та проходження квізів з **React 19**, **TypeScript**, **Redux Toolkit**, **Node.js** та **Express**.

## Технології

### Frontend
- **React 19** - сучасна UI бібліотека з новими можливостями
- **TypeScript 5.9** - строга типізація для надійного коду
- **Redux Toolkit 2.11** - сучасне управління станом з RTK Query
- **React Router 7** - клієнтська навігація (SPA)
- **Vite 7** - блискавична збірка та HMR

### Backend
- **Node.js** - JavaScript runtime середовище
- **Express 4** - мінімалістичний веб-фреймворк
- **File-based storage** - збереження даних у JSON файлах
- **express-validator** - валідація вхідних даних
- **CORS** - підтримка крос-доменних запитів

## 📁 Структура проекту

```
sait/
├── src/                   # Frontend код
│   ├── components/        # React компоненти
│   │   ├── Header.tsx     # Шапка сайту
│   │   ├── Footer.tsx     # Підвал
│   │   ├── QuizCard.tsx   # Картка квізу
│   │   ├── QuizzesSection.tsx
│   │   └── WelcomeSection.tsx
│   ├── pages/             # Сторінки додатку
│   │   ├── Home.tsx       # Головна сторінка
│   │   ├── CreateQuiz.tsx # Створення квізу
│   │   ├── ManageQuizzes.tsx # Керування квізами
│   │   ├── TakeQuiz.tsx   # Проходження квізу
│   │   └── Results.tsx    # Історія результатів
│   ├── store/             # Redux store
│   │   ├── index.ts       # Конфігурація store
│   │   ├── hooks.ts       # Типізовані хуки (useAppDispatch, useAppSelector)
│   │   ├── quizzesSlice.ts # Slice для квізів
│   │   └── resultsSlice.ts # Slice для результатів
│   ├── services/          # API сервіси
│   │   ├── api.ts         # Базова конфігурація API
│   │   ├── quizService.ts # Сервіс для роботи з квізами
│   │   └── resultService.ts # Сервіс для результатів
│   ├── types/             # TypeScript типи
│   │   └── index.ts       # Спільні типи (Quiz, Question, Result)
│   ├── css/               # Стилі
│   │   ├── variables.css  # CSS змінні
│   │   ├── base.css       # Базові стилі
│   │   ├── animations.css # Анімації
│   │   └── ...            # Модульні стилі компонентів
│   ├── App.tsx            # Головний компонент
│   └── main.tsx           # Точка входу
├── server/                # Backend код
│   ├── models/            # Моделі даних
│   │   ├── Quiz.js        # Модель квізу
│   │   └── Result.js      # Модель результату
│   ├── routes/            # Express маршрути
│   │   ├── quizzes.js     # CRUD для квізів
│   │   └── results.js     # CRUD для результатів
│   ├── data/              # Сховище даних
│   │   ├── quizzes.json   # Файл з квізами
│   │   ├── results.json   # Файл з результатами
│   │   └── storage.js     # Утиліти для роботи з файлами
│   ├── server.js          # Express сервер
│   ├── seed.js            # Скрипт заповнення тестовими даними
│   └── package.json       # Backend залежності
├── index.html             # HTML шаблон
├── vite.config.ts         # Конфігурація Vite
├── tsconfig.json          # Конфігурація TypeScript
├── package.json           # Frontend залежності
├── README.md              # Документація
├── QUICK-START.md         # Швидкий старт
├── BACKEND-GUIDE.md       # Гайд по backend
├── SETUP-INSTRUCTIONS.md  # Інструкції встановлення
└── DEFENSE.md             # Відповіді на питання для захисту
```

## 🛠 Встановлення та запуск

### Передумови
- **Node.js 18+** - [Завантажити](https://nodejs.org/)
- **npm** або **yarn** - включено з Node.js

### 1. Клонування репозиторію
```bash
git clone <repository-url>
cd sait
```

### 2. Backend Setup

```bash
# Перейдіть в папку server
cd server

# Встановіть залежності
npm install

# Заповніть БД тестовими даними
npm run seed

# Запустіть сервер
npm run dev
```

Backend буде доступний на `http://localhost:5000`

**Примітка:** Backend використовує файлове сховище (JSON файли), тому MongoDB не потрібен.

### 3. Frontend Setup

```bash
# Поверніться в кореневу папку
cd ..

# Встановіть залежності
npm install

# Запустіть frontend
npm run dev
```

Frontend буде доступний на `http://localhost:5173` або `http://localhost:3000`

### Швидкий запуск (один термінал)
```bash
# В корені проекту
cd server && npm install && npm run seed && npm run dev &
cd .. && npm install && npm run dev
```

## Доступні команди

### Frontend
```bash
npm run dev      # Запуск dev сервера з HMR
npm run build    # Production збірка (TypeScript + Vite)
npm run preview  # Перегляд production збірки
npm run lint     # Перевірка коду ESLint
npm run lint:fix # Автоматичне виправлення помилок ESLint
```

### Backend
```bash
cd server
npm run dev      # Запуск з nodemon (auto-reload)
npm start        # Production запуск
npm run seed     # Заповнити БД тестовими даними
```

## 🌐 API Endpoints

### Health Check
- `GET /api/health` - Перевірка стану сервера

### Quizzes
- `GET /api/quizzes` - Отримати всі квізи
- `GET /api/quizzes/:id` - Отримати квіз за ID
- `POST /api/quizzes` - Створити новий квіз
  ```json
  {
    "name": "Назва квізу",
    "description": "Опис квізу",
    "questions": [...]
  }
  ```
- `PUT /api/quizzes/:id` - Оновити квіз
- `DELETE /api/quizzes/:id` - Видалити квіз

### Results
- `GET /api/results` - Отримати всі результати
- `GET /api/results/:id` - Отримати результат за ID
- `POST /api/results` - Зберегти результат
  ```json
  {
    "quizId": "123",
    "quizName": "Тест з JavaScript",
    "score": 8,
    "totalQuestions": 10,
    "percentage": 80,
    "userName": "Користувач",
    "date": "2025-12-23T10:00:00.000Z"
  }
  ```
- `DELETE /api/results/:id` - Видалити результат
- `DELETE /api/results` - Видалити всі результати

## 🗺 Маршрути (Routes)

| Шлях | Компонент | Опис |
|------|-----------|------|
| `/` | `Home.tsx` | Головна сторінка з переліком доступних квізів |
| `/create` | `CreateQuiz.tsx` | Форма створення нового квізу |
| `/manage` | `ManageQuizzes.tsx` | Управління (редагування/видалення) квізами |
| `/quiz/:id` | `TakeQuiz.tsx` | Сторінка проходження конкретного квізу |
| `/results` | `Results.tsx` | Історія результатів всіх пройдених квізів |

**Навігація:** Використовується React Router v7 для SPA навігації без перезавантаження сторінки.

## 🏗 Архітектура

### State Management (Redux Toolkit)

Додаток використовує **Redux Toolkit** для централізованого управління станом:

```typescript
// store/index.ts
const store = configureStore({
  reducer: {
    quizzes: quizzesReducer,  // Управління квізами
    results: resultsReducer,   // Управління результатами
  },
});

export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;
```

**Slices:**
- `quizzesSlice.ts` - CRUD операції з квізами, async thunks для API запитів
  - `fetchQuizzes` - завантаження всіх квізів
  - `createQuiz` - створення нового квізу
  - `updateQuiz` - оновлення квізу
  - `deleteQuiz` - видалення квізу
  - `setSelectedQuiz` - вибір поточного квізу
  
- `resultsSlice.ts` - збереження та управління результатами
  - `fetchResults` - завантаження всіх результатів
  - `saveResult` - збереження результату проходження
  - `deleteResult` - видалення результату
  - `clearAllResults` - очищення всіх результатів

### Services Layer

API сервіси забезпечують комунікацію з backend:

```typescript
// services/api.ts - базова конфігурація
const API_URL = 'http://localhost:5000/api';

// services/quizService.ts - робота з квізами
export const quizService = {
  getAllQuizzes,
  getQuizById,
  createQuiz,
  updateQuiz,
  deleteQuiz
};

// services/resultService.ts - робота з результатами
export const resultService = {
  getAllResults,
  saveResult,
  deleteResult,
  clearAllResults
};
```

### Типи даних (TypeScript)

```typescript
// types/index.ts

// Базовий тип квізу
interface Quiz {
  id: string;
  name: string;
  description?: string;
  questions: Question[];
  createdAt?: string;
}

// Питання квізу
interface Question {
  id: number;
  text: string;
  type: 'single' | 'multiple' | 'text';
  options: QuestionOption[];
  correctAnswer?: string | string[];
}

// Варіант відповіді
interface QuestionOption {
  id: number;
  text: string;
  isCorrect: boolean;
}

// Результат проходження квізу
interface QuizResult {
  id: string;
  quizId: string;
  quizName: string;
  score: number;
  totalQuestions: number;
  percentage: number;
  userName?: string;
  date: string;
  answers?: UserAnswer[];
}

// Відповідь користувача
interface UserAnswer {
  questionId: number;
  questionText: string;
  userAnswer: string | string[];
  correctAnswer: string | string[];
  isCorrect: boolean;
}
```

### Backend Архітектура

```
server/
├── models/          # Моделі даних (класи)
├── routes/          # Express маршрути (REST API)
├── data/            # Файлове сховище + утиліти
└── server.js        # Головний файл сервера
```

**Особливості:**
- Файлове сховище (JSON) замість бази даних
- RESTful API endpoints
- Валідація даних через express-validator
- CORS для крос-доменних запитів
- Middleware для обробки помилок

## Особливості та функціонал

### Frontend
- **TypeScript** - повна типізація всього коду
- **Redux Toolkit** - сучасний state management з async thunks
- **React 19** - найновіша версія з покращеннями продуктивності
- **Vite** - блискавична збірка та Hot Module Replacement (HMR)
- **React Router 7** - клієнтська маршрутизація (SPA)
- **Responsive дизайн** - адаптивний інтерфейс для всіх пристроїв
- **CSS модулі** - організована структура стилів з змінними
- **Анімації** - плавні переходи та ефекти

### Типи питань
1. **Single Choice** - одна правильна відповідь (radio buttons)
2. **Multiple Choice** - кілька правильних відповідей (checkboxes)
3. **Text Answer** - розгорнута текстова відповідь

### CRUD операції
- **Create** - створення нових квізів з необмеженою кількістю питань
- **Read** - перегляд списку квізів та деталей кожного
- **Update** - редагування існуючих квізів
- **Delete** - видалення квізів

### Додаткові можливості
- Детальна статистика результатів
- 💾 Збереження результатів на сервері
- 🔄 Автоматичне оновлення даних
- ⚡ Оптимістичні оновлення UI
- 🎨 Сучасний UI/UX дизайн
- 📱 Мобільна адаптація
- Валідація форм
- ⚠️ Обробка помилок

## � Додаткова документація

- [QUICK-START.md](QUICK-START.md) - швидкий старт після встановлення Node.js
- [SETUP-INSTRUCTIONS.md](SETUP-INSTRUCTIONS.md) - детальні інструкції встановлення
- [BACKEND-GUIDE.md](BACKEND-GUIDE.md) - повний гайд по backend частині
- [DEFENSE.md](DEFENSE.md) - відповіді на питання для захисту проекту
- [CHEAT-SHEET.md](CHEAT-SHEET.md) - шпаргалка по основним командам

## 🔧 Налаштування

### Змінити порт Frontend
У файлі `vite.config.ts`:
```typescript
export default defineConfig({
  server: {
    port: 3000, // змініть на потрібний порт
  }
});
```

### Змінити порт Backend
У файлі `server/server.js`:
```javascript
const PORT = process.env.PORT || 5000; // змініть значення
```

### API URL
API URL задається в `src/services/api.ts`:
```typescript
const API_URL = 'http://localhost:5000/api';
```

## 🐛 Вирішення проблем

### Backend не запускається
```bash
cd server
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Frontend не запускається
```bash
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Порт зайнятий
Змініть порт в налаштуваннях (див. вище) або зупиніть процес:
```bash
# Windows
netstat -ano | findstr :5000
taskkill /PID <PID> /F
